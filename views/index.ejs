<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Include the head partial. This will contain meta tags, title, and CSS link. -->
    <!-- Assuming partials are in a 'partials' subfolder relative to this file's location. -->
    <%- include('./partials/head') %>
</head>
<body>
    <!-- The main wrapper div that encompasses all visible page content -->
    <div id="wrapper">
        <!-- Include the header partial (CSE Motors logo and My Account link) -->
        <%- include('./partials/header') %>

        <!-- Include the navigation partial (Home, Custom, Sedan, etc. links) -->
        <!-- This will appear directly below the header -->
        <%- include('./partials/navigation') %>

        <!--
            The main content area of the page.
            The `<%- body %>` placeholder is where the specific content for each
            route (e.g., your Delorean display, upgrades, reviews) will be injected
            from your Node.js controller (e.g., `res.render('home', { body: someHTML })`).
        -->
        <main>
            <%- body %>
        </main>

        <!-- Include the footer partial (copyright information) -->
        <%- include('./partials/footer') %>
    </div>
</body>
</html>
