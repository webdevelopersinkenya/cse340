<% /*
    This view displays the detailed information for a single vehicle.
    The actual HTML content for the vehicle details (including image and text)
    is generated by the buildVehicleDetail utility function and passed as 'body'.
    This view relies on the main layout.ejs for its structure.
*/ %>

<% /*
    The 'title' and 'nav' variables are passed from the controller
    and used by the main layout.ejs.
*/ %>

<!-- The body variable contains the HTML string generated by buildVehicleDetail -->
<%- body %>
