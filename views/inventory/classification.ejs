<% /*
    This view displays a grid of inventory items for a specific classification.
    The 'grid' variable contains the HTML string generated by buildClassificationGrid.
*/ %>

<% /*
    The 'title' and 'nav' variables are passed from the controller
    and used by the main layout.ejs.
*/ %>

<h1><%= title %></h1>

<!-- The 'grid' variable contains the generated HTML grid -->
<%- grid %>
